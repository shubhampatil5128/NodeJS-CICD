apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs
  labels:
    app: shu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shu
  template:
    metadata:
      labels:
        app: shu
    spec:
      containers:
      - name: nodeserver
        image: shubhampatil5128/nodeapp:latest
        ports:
        - containerPort: 3000
        
        
---

apiVersion: v1
kind: Service
metadata:
  name: nodejs
spec:
  selector:
    app: shu
  type: NodePort
  ports:
  - protocol: TCP
    port: 9999
    targetPort: 3000
